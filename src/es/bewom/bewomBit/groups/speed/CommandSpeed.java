package es.bewom.bewomBit.groups.speed;
/*
import org.bukkit.ChatColor;
import org.bukkit.command.Command;
import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;

import es.bewom.bewomBit.commands.utility.CommandUtilities;
*/
public class CommandSpeed {
	/*
	public static boolean commandspeed(CommandSender sender, Command cmd, String label, String[] args) throws Exception{

		if (label.equalsIgnoreCase("speed")){
			Player craftPlayer = (Player) sender;
			if (args.length == 1){

				float speed = CommandUtilities.getFloat(args[0]);
				if (speed > 10f) {
					speed = 10f;
				}

				else if (speed < 0.0001f){
					speed = 0.0001f;
				}
				
				if (speed < 1f){
					speed = speed * 0.1f;
				}
				else{
				}
				craftPlayer.setFlySpeed(speed);
				craftPlayer.setWalkSpeed(speed);
				sender.sendMessage(ChatColor.GRAY+"Tu velocidad ahora es "+speed);
			}

			else if (args.length == 2){

			}
		}
		return false;
	}

*/
}